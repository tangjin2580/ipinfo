# 前端 Nginx Dockerfile
FROM nginx:alpine

# 复制 nginx 配置文件
COPY docker/nginx-ipinfo.conf /etc/nginx/conf.d/default.conf

# 复制前端静态文件
COPY dns-compare-tool/index.html /usr/share/nginx/html/index.html

# 暴露端口
EXPOSE 80

# 启动 Nginx
CMD ["nginx", "-g", "daemon off;"]
